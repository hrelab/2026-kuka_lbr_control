services:
  kuka_lbr_control:
    build:
      context: .
      args:
        USER_NAME: ${USER:-rosuser}
        USER_UID: ${USER_UID:-1000}
        USER_GID: ${USER_GID:-1000}
    environment:
      DISPLAY: ${DISPLAY}
    network_mode: host
    stdin_open: true
    tty: true
    command: bash
